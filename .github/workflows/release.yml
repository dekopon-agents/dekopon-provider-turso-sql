name: Release

on:
  push:
    tags:
      - "v*"

concurrency:
  group: release-${{ github.ref_name }}
  cancel-in-progress: false

jobs:
  release:
    uses: dekopon-agents/provider-workflows/.github/workflows/release.yml@c8404236c18bcff3e089677e99ec3075d14b5679 # v4
    permissions:
      contents: write
      id-token: write
      attestations: write
      packages: write
